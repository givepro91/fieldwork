@AGENTS.md

# Claude Code 전용

위 `AGENTS.md` 가 이 프로젝트 지침의 정본이다. Codex 등 다른 에이전트도 같은 파일을 읽으므로,
**지침을 고칠 때는 이 파일이 아니라 `AGENTS.md` 를 고친다.** 아래에는 Claude Code 에서만 의미가 있는 것만 둔다.

- **화면 확인은 로컬 dev 서버 + 스크린샷으로 한다.** Claude Artifact 는 반복 개발에 안 쓴다(느림).
- 이 프로젝트의 auto memory 는 `~/.claude/projects/<project>/memory/` 에 있다. 배포·계정 gotcha 가 거기 적혀 있으니 배포 작업 전에 확인한다.
